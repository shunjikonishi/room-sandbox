# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                controllers.Application.notFound(path="")
GET     /*path                           controllers.Application.notFound(path)

